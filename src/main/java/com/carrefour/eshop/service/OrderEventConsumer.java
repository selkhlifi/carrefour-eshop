package com.carrefour.eshop.service;

//import org.apache.kafka.clients.consumer.ConsumerRecord;
//import org.springframework.kafka.annotation.KafkaListener;
import org.springframework.stereotype.Service;

@Service
public class OrderEventConsumer {
/*
  @KafkaListener(topics = "eshop-orders", groupId = "eshop-group")
  public void consumeOrderEvent(ConsumerRecord<String, String> record) {
    System.out.printf("Consumed event: Key = %s, Value = %s%n", record.key(), record.value());
  }*/
}
