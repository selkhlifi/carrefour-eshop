package com.carrefour.eshop.kafka;

/*
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.testcontainers.containers.KafkaContainer;
import org.testcontainers.utility.DockerImageName;
*/
//@Configuration : not working correctly, used separate kafka server instead
//@Profile("dev") : not yet
public class KafkaDevConfig {

  /*
  @Bean
  public KafkaContainer kafkaContainer() {
    KafkaContainer kafkaContainer = new KafkaContainer(DockerImageName.parse("confluentinc/cp-kafka:7.2.1"));
    kafkaContainer.start();
    return kafkaContainer;
  }*/
}
